"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/utils/inputFormatter.ts
var inputFormatter_exports = {};
__export(inputFormatter_exports, {
  inputFormatter: () => inputFormatter
});
module.exports = __toCommonJS(inputFormatter_exports);
function inputFormatter(content) {
  let result = "";
  function walk(node) {
    const anyNode = node;
    if (typeof anyNode.text === "string") {
      result += anyNode.text;
    }
    if (anyNode.type === "choice") {
      const attrs = anyNode.attrs || {};
      const options = Array.isArray(attrs.options) ? attrs.options : [];
      const selected = attrs.selectedOption || options[0] || "";
      if (typeof selected === "string") {
        result += selected;
      }
    }
    if (Array.isArray(anyNode.content)) {
      anyNode.content.forEach((child) => walk(child));
    }
  }
  walk(content);
  return result.trim();
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  inputFormatter
});
//# sourceMappingURL=inputFormatter.js.map