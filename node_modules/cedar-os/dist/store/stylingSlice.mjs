// src/store/stylingSlice.ts
var DEFAULT_STYLING = {
  darkMode: false,
  color: "#93C5FD",
  // blue-300
  secondaryColor: "#1D4ED8",
  // blue-700
  accentColor: "#FB923C"
  // orange-400
};
var createStylingSlice = (set) => ({
  // Initial state
  styling: DEFAULT_STYLING,
  // Actions
  setStyling: (newStyling) => set((state) => ({
    styling: { ...state.styling, ...newStyling }
  })),
  toggleDarkMode: () => set((state) => ({
    styling: { ...state.styling, darkMode: !state.styling.darkMode }
  }))
});
export {
  createStylingSlice
};
//# sourceMappingURL=stylingSlice.mjs.map