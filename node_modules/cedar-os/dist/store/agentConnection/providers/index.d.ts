import { AISDKProviderImplementation } from './ai-sdk.js';
import { aM as ProviderImplementation, _ as OpenAIParams, Z as MastraParams, V as CustomParams, aT as CustomProviderConfig, $ as ProviderConfig } from '../../../index-DGEVvAGh.js';
import 'zustand';
import 'zod';
import '../../agentContext/AgentContextTypes.js';
import 'react';
import '@tiptap/core';
import '../../spellSlice/SpellTypes.js';
import 'fast-json-patch';
import '../../toolsSlice/ToolsTypes.js';

declare const providerRegistry: {
    readonly openai: ProviderImplementation<OpenAIParams, {
        provider: "openai";
        apiKey: string;
    }>;
    readonly anthropic: ProviderImplementation<OpenAIParams, {
        provider: "openai";
        apiKey: string;
    }>;
    readonly mastra: ProviderImplementation<MastraParams, {
        provider: "mastra";
        apiKey?: string;
        baseURL: string;
        chatPath?: string;
        voiceRoute?: string;
        resumePath?: string;
    }>;
    readonly 'ai-sdk': AISDKProviderImplementation;
    readonly custom: ProviderImplementation<CustomParams, {
        provider: "custom";
        config: CustomProviderConfig;
    }>;
};
type ProviderRegistry = typeof providerRegistry;
declare function getProviderImplementation<T extends ProviderConfig>(config: T): ProviderRegistry[T['provider']];

export { type ProviderRegistry, getProviderImplementation, providerRegistry };
